name: Build Docker image
on: [push]
jobs:
   Docker-Build:
     steps:
        - name: Checkout code
        - uses: actions/checkout@v3
 
        - name: Run Docker Build
        - run: Docker build -t pythonimage .